({
	loadData: function(component, event, helper) {
                helper.doLoadData(component, helper);
            },

            doAccRedirect : function(component, event, helper){
                helper.doAccRedirect(component, event);
            },

            doApprovalRedirect: function(component, event, helper){
                helper.doApprovalRedirect(component, event);
            }
})